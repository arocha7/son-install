---
- hosts: "{{ target | default ('localhost') }}"
  connection: local
  become: true
  pre_tasks:
    - package: name=python-pip state=present
    - name: assure that pyOpenSSL is installed
      pip: name=pyopenssl state=present

  roles:
    - openstackclients

