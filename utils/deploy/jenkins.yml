---
- name: "deploy JENKINS CI server to the target VM appliance"
  hosts: "{{ target | default('localhost') }}"
  become: true
  connection: local
  roles:
  - ntp
  - common
  - openjdk
  - firewall
  - nginx
  - jenkins
