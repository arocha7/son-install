---

- hosts: "{{ target | default ('localhost')  }}"
  become: true
  connection: ssh
  pre_tasks:
    - file:
        path: /var/log/ansible.log
        state: touch
        owner: "{{ lookup('env','USER') }}"
        group: "{{ lookup('env','USER') }}"
        mode: "u=rw,g=r,o=r"
  roles:
    - docker
    - sp
