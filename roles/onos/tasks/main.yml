---
# tasks file for ONOS

- name: disable SElinux
  include_tasks: "{{ lookup('env','PWD') }}/roles/common/tasks/disable-selinux.yml"

- name: disable Firewall and SElinux
  include_tasks: "{{ lookup('env','PWD') }}/roles/common/tasks/disable-firewalld.yml"

- name: installing ONOS
  include_tasks: "{{ ansible_distribution_release }}.yml"

